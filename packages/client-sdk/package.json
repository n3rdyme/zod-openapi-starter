{
  "name": "@local/client-sdk",
  "private": true,
  "version": "1.0.0",
  "main": "dist/index.mjs",
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf dist && rm -rf coverage && rm -rf src/generated",
    "build": "tsc",
    "lint": "eslint --fix --max-warnings=0 ./src",
    "start": "NODE_NO_WARNINGS=1 node --loader ts-node/esm ./src/test.mts",
    "for-each": "yarn"
  },
  "devDependencies": {
    "@local/api": "workspace:^",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}

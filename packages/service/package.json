{
  "name": "service",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild --config esbuild.config.js",
    "lint": "eslint 'src/**/*.{ts,js}' --fix"
  },
  "dependencies": {
    "fastify": "^4.0.0",
    "fastify-openapi-glue": "^2.0.0",
    "@fastify/swagger": "^4.7.0",
    "ajv-errors": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "esbuild": "^0.14.39"
  }
}